% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cto_form_docx.R
\name{cto_form_docx}
\alias{cto_form_docx}
\title{Generate a Printable Word Document of a Deployed SurveyCTO Form}
\usage{
cto_form_docx(
  form_id,
  path = NULL,
  version = NULL,
  language = NULL,
  show_metadata = FALSE,
  palette = NULL
)
}
\arguments{
\item{form_id}{A character string specifying the SurveyCTO form ID.}

\item{path}{Optional character string giving the output file path. Must
end in \code{.docx}. If \code{NULL} (default), the file is written to \code{tempdir()}
under the form ID.}

\item{version}{Optional string specifying a particular form version,
passed to \code{\link[=cto_form_definition]{cto_form_definition()}}. If \code{NULL} (default), the currently
deployed version is used.}

\item{language}{Optional string naming the label language to render, for
example \code{"English (en)"}. If \code{NULL} (default), the form's
\code{default_language} is used, falling back to the first label column.}

\item{show_metadata}{Logical. If \code{TRUE}, \code{calculate} and metadata fields
such as \code{start}, \code{end}, \code{deviceid} and the audit fields are included.
Defaults to \code{FALSE}, which is what makes the output a collector's-eye
view of the form.}

\item{palette}{Optional named character vector of hex colours overriding
the defaults returned by \code{\link[=cto_docx_palette]{cto_docx_palette()}}. Names that are not
supplied keep their default colour.}
}
\value{
A data frame of the rendered fields, returned invisibly. The
document is written to \code{path} for its side effect.
}
\description{
Downloads the XLSForm definition of a form and renders the fields an
enumerator actually sees as a formatted Word table, for review and
sign-off. Calculates, metadata fields and disabled rows are left out,
groups and repeat groups become banded section headers, and every row is
shaded according to its field type.
}
\details{
\code{cto_form_printable()} asks the SurveyCTO server for its own printable
rendering of a form. \code{cto_form_docx()} is different: it builds the
document locally from the XLSForm definition, so it can show the variable
names, relevance and constraint expressions that a reviewer needs and the
server-side printable does not carry.

Fields are shaded by family rather than by raw type, so that
\verb{select_one yes_no} and \verb{select_one gender} share a colour. The families
are \code{note}, \code{text}, \code{numeric}, \code{select_one}, \code{select_multiple},
\code{datetime}, \code{geo}, \code{media} and \code{other}, with separate band colours for
groups and repeat groups.

Nesting depth is counted before any row is dropped, so a question still
shows the indentation of the groups it sits inside even when one of those
wrappers is filtered out. A group's own relevance travels with its band,
since a condition that governs the whole section would otherwise be lost.
}
\examples{
\dontrun{
# Review document for the deployed version
cto_form_docx("household_survey", path = "household_survey_review.docx")

# A specific version, in a named language
cto_form_docx(
  "household_survey",
  path = "review_v2.docx",
  version = "20231001",
  language = "Amharic (am)"
)

# Include the calculates and metadata fields as well
cto_form_docx("household_survey", path = "full.docx", show_metadata = TRUE)
}
}
\seealso{
\code{\link[=cto_form_printable]{cto_form_printable()}} for the server's own printable version.

Other Form Management Functions:
\code{\link[=cto_docx_palette]{cto_docx_palette()}},
\code{\link[=cto_form_attachment]{cto_form_attachment()}},
\code{\link[=cto_form_data]{cto_form_data()}},
\code{\link[=cto_form_data_attachment]{cto_form_data_attachment()}},
\code{\link[=cto_form_dofile]{cto_form_dofile()}},
\code{\link[=cto_form_languages]{cto_form_languages()}},
\code{\link[=cto_form_metadata]{cto_form_metadata()}}
}
\concept{Form Management Functions}
